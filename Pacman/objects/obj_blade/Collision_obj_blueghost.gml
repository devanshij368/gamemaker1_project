// only do this in Room 3
if (room == Room3) {
    
    with (other) {
        instance_destroy(); 
        // destroy the ghost
    }
}
//other means the object (blade)

if (room == Room4) {
    
    with (other) {
        instance_destroy(); 
        // destroy the ghost
    }
}